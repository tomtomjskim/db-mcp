# ===========================================
# Database MCP Server - Environment Variables
# ===========================================

# ===========================================
# 필수 데이터베이스 연결 설정
# ===========================================
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DB=your_database_name
MYSQL_USER=your_username
MYSQL_PASSWORD=your_password

# ===========================================
# 선택적 연결 설정 (기본값 있음)
# ===========================================

# 연결 타임아웃 (밀리초)
MYSQL_CONNECTION_TIMEOUT=60000
MYSQL_ACQUIRE_TIMEOUT=60000
MYSQL_TIMEOUT=60000

# 연결 풀 설정
MYSQL_CONNECTION_LIMIT=10
MYSQL_QUEUE_LIMIT=0
MYSQL_IDLE_TIMEOUT=300000

# ===========================================
# SSL/TLS 설정 (원격 데이터베이스 - AWS RDS 등)
# ===========================================

# SSL 모드: REQUIRED, PREFERRED, DISABLED
# MYSQL_SSL_MODE=REQUIRED

# SSL 인증서 경로 (AWS RDS 등에서 필요)
# MYSQL_SSL_CA=/path/to/rds-ca-2019-root.pem
# MYSQL_SSL_CERT=/path/to/client-cert.pem
# MYSQL_SSL_KEY=/path/to/client-key.pem

# ===========================================
# 보안 및 성능 설정
# ===========================================

# 쿼리 실행 제한
MAX_QUERY_EXECUTION_TIME=30000
MAX_RESULT_ROWS=10000
MAX_RESULT_SIZE_MB=50
MAX_QUERY_LENGTH=10000

# 로깅 설정
ENABLE_QUERY_LOGGING=true

# Rate Limiting
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000

# ===========================================
# 서버 설정
# ===========================================

# MCP 서버 이름
MCP_SERVER_NAME=database-mcp

# 로그 레벨: debug, info, warn, error
LOG_LEVEL=info

# 환경: development, production
NODE_ENV=development

# ===========================================
# 캐시 설정 (선택적)
# ===========================================

# 스키마 캐시 TTL (밀리초)
# SCHEMA_CACHE_TTL=300000

# 최대 캐시 항목 수
# SCHEMA_CACHE_MAX_ENTRIES=500

# 최대 캐시 크기 (바이트)
# SCHEMA_CACHE_MAX_SIZE=104857600

# ===========================================
# AWS RDS 예시 설정
# ===========================================

# AWS RDS MySQL 연결 예시
# MYSQL_HOST=your-rds-instance.cluster-xxxxx.us-east-1.rds.amazonaws.com
# MYSQL_PORT=3306
# MYSQL_DB=production_db
# MYSQL_USER=db_user
# MYSQL_PASSWORD=secure_password
# MYSQL_SSL_MODE=REQUIRED
# MYSQL_SSL_CA=/opt/ssl/rds-ca-2019-root.pem

# ===========================================
# Azure Database for MySQL 예시 설정
# ===========================================

# Azure Database for MySQL 연결 예시
# MYSQL_HOST=your-server.mysql.database.azure.com
# MYSQL_PORT=3306
# MYSQL_DB=production_db
# MYSQL_USER=username@your-server
# MYSQL_PASSWORD=secure_password
# MYSQL_SSL_MODE=REQUIRED